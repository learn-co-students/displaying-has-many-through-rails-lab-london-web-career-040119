<h1><%= @doctor.name %></h1>
<h3><%= @doctor.department %></h3>

  <% @doctor.appointments.each do |app| %>
  <ul>
    <li><%= link_to app.patient.name, patient_path(app.patient) %><br><%= app.appointment_datetime.strftime("%B %e, %Y at %H:%M") %></li>
  </ul>
  <% end %>
